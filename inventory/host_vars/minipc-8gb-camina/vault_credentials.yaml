# become / login password for this host.
# Modelled on inventory/host_vars/minipc-8gb-test-router/vault_credentials.yaml.
#
# Generate and encrypt, then replace the placeholder below:
#
#   ansible-vault encrypt_string "$(openssl rand -hex 32)" --name 'vault_password'
#
vault_password: "TODO — run the ansible-vault encrypt_string command above"
