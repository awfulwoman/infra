ansible_host: host-mba2011
ansible_user: charlie
ansible_become_password: "{{ vault_personal_password }}"

host_type: baremetal
host_name: mba2011
host_region: ber
host_site: personal
host_ipv4: 192.168.1.27
host_pfqdn: "{{ host_name }}.{{ host_site }}.{{ host_region }}"
host_fqdn: "{{ host_pfqdn }}.{{ domain_name }}"
host_description: "Personal MacBook Air 2011"
