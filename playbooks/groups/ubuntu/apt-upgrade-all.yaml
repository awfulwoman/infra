- name: Update and upgrade apt packages
  hosts: ubuntu_servers
  tasks:
    - name: Update apt cache and upgrade packages
      become: true
      ansible.builtin.apt:
        update_cache: true
        upgrade: safe
