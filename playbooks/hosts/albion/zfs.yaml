- name: ZFS and backup stuff
  hosts: albion
  roles:
    - role: system-zfs
      tags: [system, zfs]
    - role: backups-zfs-archive-offsite
      tags: [backups, zfs]
  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
