- name: ZFS and backup stuff
  hosts: generic-8gb-backups
  roles:
    - role: system-zfs
      tags: [system, zfs]
    - role: backups-zfs-server
      tags: [backups, zfs]
  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
