- name: Backup Server (dev work)
  hosts: server-8gb-backups
  gather_facts: true
  roles:
    - role: backups-zfs-server

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
