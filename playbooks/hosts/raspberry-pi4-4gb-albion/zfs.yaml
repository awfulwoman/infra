- name: ZFS and backup stuff
  hosts: raspberry-pi4-4gb-albion
  roles:
    - role: system-zfs
      tags: [system, zfs]
    - role: backups-zfs-archive-offsite
      tags: [backups, zfs]
  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
