- name: ZFS and backup stuff
  hosts: host-belinda
  roles:
    - role: system-zfs
      tags: [system, zfs]
    - role: system-zfs-policy
      tags: [system, zfs]
    - role: backups-zfs-server
      tags: [backups, zfs]
  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
