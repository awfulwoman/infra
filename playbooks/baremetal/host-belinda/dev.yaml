- name: Backup Server (dev work)
  hosts: host-belinda
  gather_facts: true
  roles:
    - role: backups-zfs-server

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
