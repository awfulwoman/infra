# Ansible Pull

Configure a host to run ansible-pull for self-provisioning. Sets up the vault password, clones the infrastructure repository, installs Galaxy dependencies, and configures cron jobs for regular pulls and Galaxy updates.
