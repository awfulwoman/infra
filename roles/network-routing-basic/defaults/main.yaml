# WAN (internet-facing) and LAN (home-facing) interface names.
# No sensible default exists — NIC names are host-specific.
# Each host MUST override these (see host_vars); an assert enforces it.
wan_iface: ""
lan_iface: ""
