# composition-open-webui

Deploys [Open WebUI](https://openwebui.com/) as a Docker Compose service, providing a web interface for local LLM inference. Connects to an Ollama API endpoint configured via `composition_open_webui_ollama_base_url` (defaults to the Malcolm Mac Mini at `192.168.1.150:11434`).
