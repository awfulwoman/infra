# system-git

Configures global git settings for the Ansible user: `user.name`, `user.email`, `pull.rebase`, and `init.defaultBranch`. Defaults to Charlie O'Hara's identity with rebase-on-pull and `main` as the default branch.
