# Virtual QEMU Host

Configure a host to run QEMU/KVM virtual machines. Installs libvirt, QEMU, and related virtualisation tooling, adds the user to libvirt groups, and configures the default network.
