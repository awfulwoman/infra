# Bootstrap Intel MacBook

Configure an older Intel-based MacBook running Ubuntu. Installs Broadcom wireless drivers (bcmwl-kernel-source), removes conflicting kernel modules, and loads the WL module for WiFi functionality.
