#jinja2: lstrip_blocks:True, trim_blocks:True
// This is the primary configuration file for the BIND DNS server named.
//
// Please read /usr/share/doc/bind9/README.Debian for information on the
// structure of BIND configuration files in Debian, *BEFORE* you customize
// this configuration file.
//
// If you are just adding zones, please do that in /etc/bind/named.conf.local

include "/etc/bind/named.conf.options";
include "/etc/bind/named.conf.local";
{% if not bind_split_horizon_enabled %}
include "/etc/bind/named.conf.default-zones";
{% else %}
// named.conf.default-zones NOT included here: once any view exists, BIND
// requires every zone to be inside a view ("when using 'view' statements,
// all zones must be in views"). Its 5 stock zones (root hints, localhost,
// RFC1912 reverse zones) are duplicated into each view in
// named.conf.options instead - the same pattern already used for RPZ.
{% endif %}
