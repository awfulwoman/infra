# Boostraps a MacOS server

- name: Ensure Homebrew formulae are installed
  community.general.homebrew:
    name: "{{ bootstrap_macos_server_homebrew_formulae }}"
    path: "{{ system_homebrew_bin | dirname }}"
    state: present
