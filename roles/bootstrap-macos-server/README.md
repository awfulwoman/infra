# bootstrap-macos-server

Bootstraps a macOS server. Applies `system-environment`, patches `.zshrc` to normalise the `TERM` variable when connecting over SSH from Ghostty, and removes default consumer apps (GarageBand, iMovie, Keynote, Numbers, Pages).
