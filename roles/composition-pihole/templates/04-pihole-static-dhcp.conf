{% for item in host_group %}
{% if (hostvars[item].mac_address is defined) and (hostvars[item].host_ipv4 is defined) and (hostvars[item].host_name is defined) %}
dhcp-host={{hostvars[item].mac_address}},{{hostvars[item].host_ipv4}},{{hostvars[item].host_name}}
{% endif %}
{% endfor %}
