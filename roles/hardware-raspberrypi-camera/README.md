# Hardware Raspberry Pi Camera

Configure the legacy Raspberry Pi camera module. Enables the camera interface, loads the bcm2835-v4l2 kernel module, installs GStreamer packages, and sets up a camera streaming service.
