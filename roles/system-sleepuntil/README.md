# System Sleep Until

Configure automated system suspend and wake schedules. Deploys a script that uses rtcwake to suspend the system and schedule automatic wake-up, with optional cron-based auto-suspend at specified times.
