ansible_host: awfulwoman.com
ansible_user: "{{ vault_server_username }}"

# --- NETWORK -------------
configure_dns_linkdevice_physical: "eth0" 

# --- COMPOSITIONS -------------
compositions_config_dir: "/home/{{ vault_server_username }}/compositions"
static_site_server_path: /opt/sites/awfulwoman.com

compositions:
  - reverseproxy
  - container-management
  # - personal-site
  - static-site-server
