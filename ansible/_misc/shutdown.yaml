# ---
# - name: Shutdown all k8s servers and agents 
#   hosts: k8s
#   gather_facts: no
#   tasks:
#     - name: shutdown
#       community.general.shutdown:
#       sudo: true
#     - name: wait go down
#       local_action: wait_for host={{ ansible_ssh_host }} port=22 state=stopped
