bootstrap_ubuntu_timezone: "Europe/Berlin"
bootstrap_ubuntu_apt_packages:
  - curl
  - dirmngr
  - e2fsprogs
  - ffmpeg
  - git
  - htop
  - iptables
  - mosh
  - netplan.io
  - python-is-python3
  - python3
  - python3-docker
  - python3-pip
  - python3-virtualenv
  - sqlite3
  - timg
  - tmux
  - unzip
  - uptimed
  - util-linux-extra
  - vim
  - wget
  - zsh

# SSH agent configuration
bootstrap_ubuntu_ssh_agent_enable: true
bootstrap_ubuntu_ssh_key_path: "~/.ssh/id_ed25519"

# SSH config for infra hosts
bootstrap_ubuntu_ssh_config_enable: true
bootstrap_ubuntu_ssh_config_infra_group: "infra"
bootstrap_ubuntu_ssh_config_user: "{{ vault_server_username }}"
bootstrap_ubuntu_ssh_config_strict_host_key_checking: false
bootstrap_ubuntu_ssh_config_user_known_hosts_file: "/dev/null"

# Workspace path to cd into on login
bootstrap_ubuntu_workspace_path: ""
