# Monitoring Linux2MQTT

Publish system metrics to MQTT. Deploys a systemd service that reports host statistics (CPU, memory, disk, etc.) to an MQTT broker for monitoring via Home Assistant or other consumers.
