# System Docker

Install and configure Docker from the official Docker repository. Adds the Docker apt repository, installs Docker CE with Compose plugin, adds the user to the docker group, and deploys daemon configuration.
