// Local physical network only - exclude overlay network
acl LAN {
        192.168.1.0/24;
};
options {
        directory "/var/cache/bind";
        allow-query { 
                localhost; 
                LAN; 
        };
        forwarders { 
                1.1.1.1;
        };
        recursion yes;
};

zone "{{ bind_local_domain }}" {
        type master;
        file "/etc/bind/zones/db.lan.{{ bind_local_domain }}.zone";
        allow-transfer { 
                localhost; 
                LAN; 
        };
};

zone "1.168.192.in-addr.arpa" {
    type master;
    file "/etc/bind/zones/db.lan.192.168.1";
};