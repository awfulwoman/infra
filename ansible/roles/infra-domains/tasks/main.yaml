- name: Ensure domains exist
  community.digitalocean.digital_ocean_domain:
    state: present
    name: "{{ vault_domain_test }}"
    oauth_token: "{{ vault_digitalocean_full_access_token }}"
    project_name: Infra
