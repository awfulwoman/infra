- name: Loop over records and deal with each one
  ansible.builtin.include_tasks:
    file: record.yaml
  loop: "{{ item.records }}"
  loop_control:
    loop_var: domain_item
    extended: true
  when: item.records is defined
