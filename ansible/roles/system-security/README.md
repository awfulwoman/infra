# System Security

Harden server security configuration. Installs unattended-upgrades and fail2ban, configures SSH hardening (disables password auth, root login, etc.), manages sudo access, and deploys fail2ban jail configurations.
