- name: Install Wakonlan tools
  become: true
  ansible.builtin.apt:
    name:
      - wakeonlan
    state: present
    update_cache: false

# pip install paho-mqtt

# Copy script to controller
# Set script to run constantly
