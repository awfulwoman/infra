{% for item in host_group %}
{% if (hostvars[item].mac_address is defined) and (hostvars[item].ansible_host is defined) and (hostvars[item].host_name is defined) %}
dhcp-host={{hostvars[item].mac_address}},{{hostvars[item].ansible_host}},{{hostvars[item].host_name}}
{% endif %}
{% endfor %}
