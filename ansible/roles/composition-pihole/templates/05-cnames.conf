{% for item in host_group %}
{% if (hostvars[item].cnames is defined) and (hostvars[item].host_name is defined) %}
{% for cname in hostvars[item].cnames %}
cname={{cname}},{{hostvars[item].host_name}}
{% endfor %}
{% endif %}
{% endfor %}
