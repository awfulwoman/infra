- name: Ensure Terraform repo exists
  become: true
  ansible.builtin.deb822_repository:
    name: terraform
    types: deb
    uris: "https://apt.releases.hashicorp.com"
    suites: "{{ ansible_facts['distribution_release'] }}"
    components: main
    architectures: >-
      {% if ansible_facts['architecture'] == 'x86_64' %}amd64
      {% elif ansible_facts['architecture'] == 'aarch64' %}arm64
      {% else -%}ansible_facts['architecture']{% endif %}
    signed_by: "https://apt.releases.hashicorp.com/gpg"

- name: Ensure Terraform and libvirt client installed
  become: true
  ansible.builtin.apt:
    update_cache: true
    name:
      - terraform
      # - libvirt-clients  # For qemu+ssh:// connection to hypervisor

# ===================================================================
# SECURITY: Dedicated service account with restricted libvirt access
# ===================================================================

# - name: Create libvirt-remote service account on hypervisor
#   become: true
#   ansible.builtin.user:
#     name: "{{ infra_privateresources_hypervisor_user }}"
#     comment: "Terraform libvirt remote access (restricted)"
#     system: true
#     create_home: true
#     shell: /bin/sh  # Required for SSH forced command execution
#     groups: libvirt  # Only libvirt group access
#     append: false
#   delegate_to: "{{ infra_privateresources_hypervisor_host }}"

# - name: Deploy forced command script on hypervisor
#   become: true
#   ansible.builtin.copy:
#     src: libvirt-only.sh
#     dest: /usr/local/bin/libvirt-only
#     owner: root
#     group: root
#     mode: '0755'
#   delegate_to: "{{ infra_privateresources_hypervisor_host }}"

# - name: Generate dedicated SSH key pair for libvirt connection on control plane
#   community.crypto.openssh_keypair:
#     type: ed25519
#     path: "{{ infra_privateresources_ssh_key_path }}"
#     comment: "{{ infra_privateresources_hypervisor_user }}@{{ ansible_facts['hostname'] }}"
#     owner: "{{ ansible_user }}"
#     group: "{{ ansible_user }}"
#     mode: '0600'
#   register: libvirt_keypair

# - name: Read generated public key
#   ansible.builtin.slurp:
#     src: "{{ infra_privateresources_ssh_pubkey_path }}"
#   register: libvirt_pubkey

# - name: Configure restricted SSH access on hypervisor
#   become: true
#   ansible.posix.authorized_key:
#     user: "{{ infra_privateresources_hypervisor_user }}"
#     state: present
#     key: "{{ libvirt_pubkey.content | b64decode }}"
#     key_options: 'no-agent-forwarding,no-X11-forwarding'
#     comment: "Restricted libvirt access from {{ ansible_facts['hostname'] }}"
#   delegate_to: "{{ infra_privateresources_hypervisor_host }}"

# - name: Configure SSH client to use dedicated key for hypervisor
#   ansible.builtin.blockinfile:
#     path: "/home/{{ ansible_user }}/.ssh/config"
#     create: true
#     owner: "{{ ansible_user }}"
#     group: "{{ ansible_user }}"
#     mode: '0600'
#     marker: "# {mark} ANSIBLE MANAGED: libvirt-remote for {{ infra_privateresources_hypervisor_host }}"
#     block: |
#       Host {{ infra_privateresources_hypervisor_host }}
#         User {{ infra_privateresources_hypervisor_user }}
#         IdentityFile {{ infra_privateresources_ssh_key_path }}
#         IdentitiesOnly yes

# - name: Add hypervisor to known_hosts
#   ansible.builtin.known_hosts:
#     name: "{{ infra_privateresources_hypervisor_host }}"
#     key: "{{ lookup('pipe', 'ssh-keyscan -H ' + infra_privateresources_hypervisor_host) }}"
#     state: present
#     path: "/home/{{ ansible_user }}/.ssh/known_hosts"

# ===================================================================
# Cloud-init configuration
# ===================================================================

- name: Read Github authorized keys as string
  ansible.builtin.uri:
    url: https://github.com/awfulwoman.keys
    method: GET
    return_content: true
  register: github_keys_string

- name: Ensure Github keys list exists
  ansible.builtin.set_fact:
    github_keys_list: "{{ github_keys_string.content | split('\n') | reject('==', '') }}"

- name: Ensure Terraform project working dir exists
  become: true
  ansible.builtin.file:
    dest: "{{ infra_privateresources_terraform_working_dir }}"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0755"

- name: Ensure cloud-init file exists
  ansible.builtin.template:
    src: cloud-init
    dest: "{{ infra_privateresources_terraform_working_dir }}/cloud-init.yaml"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0644"

- name: Ensure Terraform templates exist
  ansible.builtin.template:
    src: "{{ item }}"
    dest: "{{ infra_privateresources_terraform_working_dir }}/{{ item | regex_replace('\\.j2$', '') }}"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0644"
  loop:
    - main.tf
    - provider.tf
    - variables.tf
    - vms.tf.j2
  register: tf_files

- name: Deploy terraform wrapper script
  ansible.builtin.template:
    src: tf.sh
    dest: "{{ infra_privateresources_terraform_working_dir }}/tf"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0755"

- name: DEBUG TF files
  ansible.builtin.debug:
    var: tf_files
  when: infra_privateresources_debug | bool

- name: Run Terraform
  when: infra_privateresources_tf_apply | bool
  block:
    - name: Ensure Terraform plan is tested
      community.general.terraform:
        project_path: "{{ infra_privateresources_terraform_working_dir }}"
        state: present
        force_init: true
        variables:
          hypervisor_uri: "{{ infra_privateresources_hypervisor_ssh_uri }}"
      register: tf_plan
      check_mode: true

    - name: DEBUG TF plan
      ansible.builtin.debug:
        var: tf_plan
      when: infra_privateresources_debug | bool

    - name: Ensure Terraform plan is applied
      when: (tf_plan.changed) and (not tf_plan.failed)
      community.general.terraform:
        project_path: "{{ infra_privateresources_terraform_working_dir }}"
        state: present
        variables:
          hypervisor_uri: "{{ infra_privateresources_hypervisor_ssh_uri }}"
