backends:
  hdd:
    type: local
    path: /mnt/backups/restic # a mounted NFS share
    key: "{{vault_backup_key}}"
locations:
{% for item in backup_nfs %}
  {{item}}:
      from:
        - /home/ubuntu/{{item}}
      to:
        - hdd
      cron: "{{ cron_schedule }}"
{% endfor %}
version: 2
