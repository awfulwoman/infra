# You can find documentation for all the supported env variables at https://immich.app/docs/install/environment-variables
UPLOAD_LOCATION="/slowpool/charlie/photos" # The location where your uploaded files are stored
DB_DATA_LOCATION="{{ container_config_dir }}/{{ container_name }}/database" # The location where your database files are stored
IMMICH_VERSION=release # The Immich version to use. You can pin this to a specific version like "v1.71.0"
DB_PASSWORD=postgres # Connection secret for postgres. You should change it to a random password
TZ="Europe/Berlin"

# The values below this line do not need to be changed
###################################################################################
DB_USERNAME=postgres
DB_DATABASE_NAME=immich