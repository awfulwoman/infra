# Compositions

Orchestrate Docker Compose applications. Creates a shared Docker bridge network, provisions ZFS datasets for each composition, and dynamically includes the appropriate `composition-*` roles based on the `compositions` list variable.
