
- name: Get all 
  ansible.posix.synchronize:
    src: "{{ item.uri }}"
    dest: "/slowpool/images/{{ item.name }}"
    archive: true
    links: true
    delay_updates: false
    partial: true
  loop: "{{ resources }}"