
- name: Synchronization without --archive options enabled except use --links
  ansible.posix.synchronize:
    src: rsync://mirror.kkg.berlin/ubuntu-releases/24.04/
    dest: /slowpool/images/ubuntu/ubuntu/temp
    archive: true
    links: true
    delay_updates: false
    partial: true