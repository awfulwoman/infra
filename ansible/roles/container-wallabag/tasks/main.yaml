- name: Set config path
  ansible.builtin.set_fact:
    container_config_dir: "{{ ansible_env.HOME }}"

- name: "Create {{ container_name }} config dir"
  ansible.builtin.file:
    path: "{{ container_config_dir }}/wallabag/data"
    state: directory

- name: "Create {{ container_name }} config dir"
  ansible.builtin.file:
    path: "{{ container_config_dir }}/wallabag/images"
    state: directory

- name: "Create {{ container_name }} container"
  # become: true
  community.docker.docker_container:
    name: wallabag
    image: wallabag/wallabag
    state: started
    restart_policy: unless-stopped
    ports:
      - "7520:80"
    env:
      TZ: "Berlin/Europe"
      SYMFONY__ENV__DOMAIN_NAME: "http://workloads.affordablepotatoes.com:7520" 
    volumes:
      - "{{ container_config_dir }}/wallabag/data:/var/www/wallabag/data"
      - "{{ container_config_dir }}/wallabag/images:/var/www/wallabag/web/assets/images "
      - /etc/localtime:/etc/localtime:ro
