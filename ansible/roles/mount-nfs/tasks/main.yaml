- name: Mount backup NFS volumes
  become: true
  ansible.posix.mount:
    src: 192.168.1.116:/slowpool/backups # hardcoded for now
    path: /mnt/backups
    opts: rw,all_squash
    state: mounted
    fstype: nfs