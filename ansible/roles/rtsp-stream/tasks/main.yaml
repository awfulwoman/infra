# Ensure video4linux is installed

# ensure ffmpeg is installed

# ensure tiny web server is installed (for serving thumbnail)

# set up rtsp server service
# ffmpeg -f v4l2 -i /dev/video0 -pix_fmt yuv420p -preset ultrafast -b:v 600k -f rtsp rtsp://localhost:$RTSP_PORT/$RTSP_PATH -vframes 1 -y thumbnail.jpg

# set up web server service
