{% if bind_dnssec_keys is defined and bind_dnssec_keys | length > 0 %}
{% for key in bind_dnssec_keys %}
key {{ key.name}} {
    algorithm {{ key.algorithm}};
    secret "{{ key.secret }}";
};

{% endfor %}
{% endif %}
