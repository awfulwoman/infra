# code: language=ansible
name: "{{ composition_name }}"
services:
  web:
    image: nginx
    # image: lipanski/docker-static-website:latest
    container_name: awfulwoman_beta
    # build: ./
    ports:
      - "8081:80"
      # - "3000:3000"
    env_file: .environment_vars
    environment:
      - NGINX_HOST="beta.awfulwoman.com"
      - NGINX_PORT=80
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.beta.rule=Host(`beta.awfulwoman.com`)"
      - "traefik.http.routers.beta.tls=true"
      - "traefik.http.routers.beta.tls.certresolver=letsencrypt"
    volumes:
      - "/site:/usr/share/nginx/html"
      - "{{ composition_config }}/nginx.conf:/etc/nginx/nginx.conf"
      - "{{ composition_config }}/logs:/var/log/nginx"
      - /etc/localtime:/etc/localtime:ro
    networks:
      - "{{ default_docker_network }}"

networks:
  "{{ default_docker_network }}":
    external: true
