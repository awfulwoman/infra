---
# Home Automation playbook
# Assumes a bare install of Raspbian or Ubuntu

- name: Home Assistant stuff
  hosts: ha_homeassistant
  roles:
    - role: standard_os
    - role: geerlingguy.docker_arm
    - role: homeassistant
    - role: ddns
    - role: upgrade
    # - role: gatherfacts
    # - role: serdigital64.bkp_restic
  handlers:
    - name: Reboot host
      become: true
      reboot:
