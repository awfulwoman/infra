- name: Backup Server (dev work)
  hosts: host-backups
  gather_facts: true
  roles:
    - role: backups-zfs-server-new

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
