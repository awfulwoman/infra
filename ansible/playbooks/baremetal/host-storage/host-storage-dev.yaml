- name: Storage NAS
  hosts: host-storage
  # vars:
  #   compositions:
  #     - libretranslate
  roles:
    - role: configure-dns-server

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
