- name: Storage NAS
  hosts: host-storage
  # vars:
  #   compositions:
  #     - libretranslate
  roles:
    - role: system-netplan

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
