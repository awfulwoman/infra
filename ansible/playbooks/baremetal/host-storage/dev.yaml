- name: Storage NAS
  hosts: host-storage
  # vars:
  #   compositions:
  #     - libretranslate
  roles:
    - role: infra-private-resources

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
