- name: Storage NAS
  hosts: host-storage
  # vars:
  #   compositions:
  #     - libretranslate
  roles:
    # - role: infra-private-resources
    - role: system-zfs

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
