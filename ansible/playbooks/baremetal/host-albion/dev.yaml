- name: UK Server (dev work)
  hosts: host-albion
  roles:
    # - role: compositions
    - role: system-ubuntupro

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
