- name: DNS
  hosts: dns
  roles:
    - role: weareinteractive.environment
      become: true
    - role: infra-domains

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
