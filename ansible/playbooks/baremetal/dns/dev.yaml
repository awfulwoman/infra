- name: DNS
  hosts: dns
  roles:
    # - role: weareinteractive.environment
    #   become: true
    - role: infra-public-resources

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
