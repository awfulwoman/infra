- name: DNS
  hosts: dns
  roles:
    # - role: weareinteractive.environment
    #   become: true
    - role: script-share-personal-site

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
