- name: Out on the internet server
  hosts: vm-awfulwoman
  # vars:
  #   compositions:
  #     - awfulwoman
  roles:
    # - role: compositions
    - role: system-zfs

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
