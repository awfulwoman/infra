- name: Out on the internet server
  hosts: vm-awfulwoman-hetzner
  # vars:
  #   compositions:
  #     - awfulwoman
  roles:
    # - role: compositions
    - role: system-zfs

  handlers:
    - name: Reboot host
      become: true
      ansible.builtin.reboot:
