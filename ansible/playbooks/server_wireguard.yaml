---
# Wireguard Server - entrypoint for LAN
# Assumes a bare install of Raspbian or Ubuntu

- name: Wireguard server
  hosts: wireguard
  become: true
  roles:
    - role: raspberry_pi
    - role: standard_os
    # - role: githubixx.harden-linux
    - role: githubixx.ansible_role_wireguard
    - role: wireguard
  handlers:
    - name: Reboot host
      become: true
      reboot:
