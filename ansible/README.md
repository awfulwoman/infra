# Ansible

A bunch of Ansible playbooks that I use to manage my home hardware.

## Installation

```bash
ansible-galaxy install -r ansible/meta/requirements.yaml
```

## Using playbooks

```bash
ansible-playbook ansible/playbooks/baremetal/host-storage.yaml
```
