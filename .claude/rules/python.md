# Python

`ansible` and `pre-commit` are installed via Homebrew. No venv is needed for this project.

Python scripts defined within Ansible roles are an exception — they will have their own virtual environments defined in the Ansible tasks.
